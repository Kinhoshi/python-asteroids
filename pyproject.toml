[project]
name = "python-asteroids"
version = "0.5.0"
description = "Asteroids clone remade in Python language with a few modern twists"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
    "pygame-menu>=4.5.2",
]
